package Encapsulation.Parent;

public class Parent {
     // Protected member variable
    protected String protectedMessage = "This is protected data from ParentClass";

    // Protected method
    protected void showProtectedMessage() {
        System.out.println(protectedMessage);
    }
}
